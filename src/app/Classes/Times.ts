export class Times {
  start: number;
  end: number;
  progress: number;

  constructor(start: number, end: number, progress: number) {
    this.start = start;
    this.end = end;
    this.progress = progress;
  }
}
